---
name: case-review
description: 独立测试用例评审（Excel/XMind/Markdown）。用例文件必选，需求/接口/设计文档可选。四维内部分析，输出 Markdown 报告（含待澄清事项与整改建议）；评审后引导用户澄清并确认是否修改原 Excel 或新建文件。触发词：用例评审、case review、速评、评审用例。
---

# 独立测试用例评审 用例评审（case-review）

## 定位

**独立 Skill**：用户可在任意目录提供用例文件即可评审。

**落地优先**：输出 3 类问题分类表格 + 待澄清事项 + 整改建议汇总；涉及补用例/改写的，在整改建议中写清前置、步骤、预期即可。详细结论写入 Markdown 报告，对话侧仅摘要待澄清项与可执行整改要点。

**默认可推断**：未提供需求文档时，按通用测试规范 + 用例内业务语义评审。

## 适用场景

- 只有 Excel / XMind 用例，没有 PRD，想快速自查漏场景、写得乱、能不能直接执行
- 有用例 + 需求/接口文档，想做对标需求的评审
- 需要指出模糊用例、遗漏场景、重复可合并项

## 输入

### 必填（至少一项）

| 类型 | 说明 |
|------|------|
| Excel `.xlsx` | 推荐；含用例编号、名称、步骤、预期等列 |
| XMind `.xmind` | XMind 8 格式思维导图用例 |
| Markdown | 分段描述的用例文档 |

### 可选

| 类型 | 说明 |
|------|------|
| PRD / 需求说明 | 有则做需求↔用例对标；无则做场景自洽性检查 |
| 接口文档 | 有则校验接口字段、错误码、异常响应覆盖 |
| 概要设计 | 有则校验跨模块链路与状态流转 |
| 输出路径 | 用户未指定时，按「输出文件命名规则」自动生成 |
| 侧重点 | 如「只查边界」「只查步骤规范性」 |

## 解析用例（必须先执行）

优先用内置脚本结构化读取，避免大文件漏读：

### Excel

```bash
python3 .claude/skills/opentest-case-review/scripts/read_cases_excel.py --xlsx <用例路径>
```

可选：`--sheet 测试用例`

### XMind

```bash
python3 .claude/skills/opentest-case-review/scripts/read_cases_xmind.py --xmind <用例路径>
```

### Markdown

Agent 直接读取文件；若同时提供 Excel/XMind，以脚本 JSON 为准。

脚本输出 JSON 含 `cases` 数组，每条含 `case_id`/`name`/`row`/`path`、`preconditions`、`steps`、`expected` 等字段，评审时引用这些标识定位问题。

## 依赖

- **Excel 读取**：`pip install openpyxl`
- **XMind 读取**：标准库即可（zipfile + xml）

## 评审维度（4 项，内部分析用）

从以下四维评审，将原「测试数据」「可追溯性」「可维护性」中有用内容融入对应维度，**输出时不单独出四维检查清单**，只归类到下方 3 类问题表格。

### 1. 需求与场景覆盖完整性

- 有需求文档：主流程、分支、角色、异常、回滚是否都有对应用例；重要需求是否漏测
- 无需求文档：从用例反推业务，检查主流程、关键分支、多角色/多渠道、失败与回滚是否齐全

### 2. 用例步骤与预期描述规范性

- 前置是否写清；步骤是否按序、可复现；预期是否具体可验证
- 拒绝「正常」「成功」「无异常」等模糊预期

### 3. 边界值与异常场景覆盖

- 输入边界、特殊字符、权限异常、非法状态、重复提交
- 依赖超时、接口失败、资源不足等

### 4. 用例可执行性与冗余重复

- 环境、账号、数据依赖是否能在现网/测试环境直接执行
- 命名与分组是否清晰；是否存在重复或高度相似用例

### 问题条目格式

按 3 类问题填表格，每行「问题说明 + 整改建议」；风险可在问题说明中一句带过。文件/字段层面问题（模块列、追溯列等）不单独成章，测试数据类归入「用例质量问题」，其余可省略。

## 输出格式

### 输出文件命名规则

**禁止**写死 `case-review.md` 等固定名。按以下优先级确定路径：

1. **用户指定路径** → 直接用（如 `docs/支付评审.md`）
2. **有 PRD/需求文档** → `<需求主文件名>_用例评审.md`（与需求文档同目录，除非用户指定其他目录）  
   例：`支付模块PRD.md` → `支付模块PRD_用例评审.md`
3. **仅用例文件** → `<规范化主名>_用例评审.md`（与用例文件同目录）  
   例：`公寓管理系统_测试用例.xlsx` → `公寓管理系统_用例评审.md`  
   例：`login-cases.xlsx` → `login-cases_用例评审.md`
4. **Markdown 用例** → `<md主文件名>_用例评审.md`

**主名规范化**：去掉扩展名及 `_测试用例`、`-cases`、`_cases` 等冗余后缀后再拼接 `_用例评审.md`，避免文件名过长重复。

报告标题 `# 用例评审 - [名称]` 与文件名主名保持一致。

### 问题分类（3 类）

评审后归入以下类别，无问题的类别可省略。

| 类别 | 放什么 |
|------|--------|
| **缺失用例** | 应有但完全没有的场景（覆盖缺口、边界/异常漏测） |
| **用例质量问题** | 已有用例写得不对、测不准（步骤/预期/前置/数据） |
| **重复与冗余** | 高度相似、可合并或删减的用例 |

### 正文结构

```markdown
# 用例评审 - [名称]

- **用例来源**：[路径]（N 条）
- **需求文档**：[有/无，路径]
- **评审日期**：[YYYY-MM-DD]

## 整体结论

（1–3 句：能否直接执行、主要缺口、是否建议补测后再跑）

## 问题分析

### 缺失用例

| 场景/模块 | 问题说明 | 优先级 | 整改建议 |
|-----------|----------|--------|----------|
| … | … | P0/P1 | 补充用例：… |

### 用例质量问题

| 用例 ID | 问题说明 | 整改建议 |
|---------|----------|----------|
| TC-007 | … | … |

### 重复与冗余

| 相似组 | 建议保留 | 建议删除/合并 | 理由 |
|--------|----------|---------------|------|
| … | … | … | … |

## 待澄清事项

（仅列需产品/用户确认后才能定稿的规则或行为；每条一句 + 注明影响哪些用例；无则写「无」）

## 整改建议汇总

（3–5 条 Agent 或测试同学可直接执行的要点：补哪类、改哪条、删/并哪组；不含待澄清项）
```

**表格原则**：每行一件事；整改建议写具体动作（补哪类、改哪条、删/并哪组）；缺失用例保留优先级列。

**待澄清 vs 整改建议归类**：

| 归入待澄清 | 归入整改建议汇总 |
|-----------|-----------------|
| 需求与用例/实现不一致，定稿前需确认 | 规则已明确、可直接改 Excel 的动作 |
| 提示文案、删除/状态联动规则未定义 | 统一前置、补具体用例、删/并冗余条 |
| 例：退房时间是「不早于入住时间」还是「不早于当前时间」（影响 TC-024/025） | 例：统一 TC-005 前置为「已登录运营方账号」、删除 TC-032 |

`## 整体结论` 与三类问题表格 **只写在报告中**，不在对话里复述。

## 评审完成后的对话回复

写完 Markdown 报告后，**对话回复须简短**，引导用户澄清并确认是否改 Excel。详细分析已在报告中，禁止在聊天里重复。

### 对话中写 / 不写

| 对话中写 | 对话中不写 |
|---------|-----------|
| 报告路径（Markdown 链接） | 整体结论全文 |
| 待澄清事项（从报告摘录，通常 2–5 条） | 三类问题表格 |
| 整改建议要点（3–5 条 bullet，可执行项） | 「主要发现」式长摘要 |
| 一句说明：「详细分析见报告」 | 重复报告中的优先级/模块清单 |

### 固定结尾：确认是否改 Excel

用户**未**在同轮消息中明确改法时，须用 `AskQuestion` 询问下一步：

| 选项 | 含义 |
|------|------|
| **修改原 Excel** | 就地改写/增补/删除，覆盖原文件 |
| **新建 Excel** | 保留原文件，输出新文件（默认 `<原主名>_整改.xlsx`，或用户指定路径） |
| **暂不修改** | 仅保留评审报告 |

用户已在同轮消息中明确选择时，跳过 `AskQuestion`，按选择执行。若待澄清项尚未确认且会阻塞改写，先列出待澄清项；用户要求继续时可注明「按报告假设执行」。

### 对话回复模板

```markdown
评审报告已写入 [`<报告路径>`](<报告路径>)，详细问题与表格见文档。

**待澄清**
1. …（影响 TC-xxx）
2. …

**整改建议**
- …
- …

请确认是否按建议修改用例文件？
```

随后调用 `AskQuestion` 提供三选一（修改原 Excel / 新建 Excel / 暂不修改）。

## 标准流程（Agent 执行顺序）

1. **确认输入**：用例路径、可选文档、侧重点；**推断输出文件路径**（用户指定 > PRD 名 > 用例名）
2. **解析用例**：运行 `read_cases_excel.py` 或 `read_cases_xmind.py`，得到 JSON
3. **读可选文档**：有 PRD/接口/设计则提取功能点、规则、异常；无则跳过
4. **粗映射**：有文档时建立需求↔用例对应，标空白与冗余；无文档时做场景完整性启发式检查
5. **四维评审**：按维度发现问题，再归类到 3 类（缺失用例 / 用例质量问题 / 重复与冗余），填入对应表格；区分待澄清项与可执行整改项
6. **生成产物**：分类表格 + 待澄清事项 + 整改建议汇总
7. **写入文件**：保存到步骤 1 推断/指定的路径
8. **简短回复**：按「评审完成后的对话回复」规范回复；用户未明确改法时用 `AskQuestion` 确认
9. **用户确认后改 Excel**：若用户选择修改原 Excel 或新建 Excel，再执行补写/改写；评审阶段默认不自动改原文件

## 使用示例

### 示例 1：仅 Excel

> 帮我评审 `公寓管理系统_测试用例.xlsx`，没有需求文档，快速看看有没有漏场景和写得糊的地方。

- 运行 `read_cases_excel.py`
- 输出 `公寓管理系统_用例评审.md`（与用例同目录）

### 示例 2：Excel + PRD

> 这是 `支付模块PRD.md` 和 `payment-cases.xlsx`，对照需求评审用例。

- 读 PRD + Excel JSON，做需求↔用例对标
- 输出 `支付模块PRD_用例评审.md`

### 示例 3：XMind，侧重边界

> `order-flow.xmind`，重点看边界和异常有没有漏。

- 运行 `read_cases_xmind.py`
- 输出 `order-flow_用例评审.md`，维度 3 为重点，其他维度简要带过

### 示例 4：用户指定输出路径

> 评审 `login-cases.xlsx`，结果写到 `docs/review/login-review.md`。

- 直接使用用户指定路径

### 示例 5：评审完成后的期望对话

报告已写入 `test-data/需求文档_用例评审.md` 后，对话仅回复：

```markdown
评审报告已写入 [`test-data/需求文档_用例评审.md`](test-data/需求文档_用例评审.md)，详细问题与表格见文档。

**待澄清**
1. 退房时间校验规则：不早于入住时间 vs 不早于当前时间（影响 TC-024/025）
2. 入住后房间状态是否联动（影响 TC-021）
3. 有关联在住/工单时是否允许删除公寓/房间（影响 TC-007）

**整改建议**
- 统一负向用例前置为「已登录运营方账号」
- P0 补：数据隔离、级联删除、跨模块冒烟
- 删 TC-032 或收窄为单模块抽检
```

随后 `AskQuestion`：修改原 Excel / 新建 Excel / 暂不修改。

## 注意事项

- 信息略有缺失时不要反复追问；在报告中写明假设前提，待澄清项写入「待澄清事项」
- 语言用测试同事日常说法，避免架构评审式空话
- 整改建议中涉及补用例/改写的，字段与 OpenTest 生成格式对齐（前置条件、测试步骤、预期结果、优先级 P0/P1/P2）
- **评审阶段只产出 Markdown**，不自动改 Excel；改 Excel 须用户确认「修改原 Excel / 新建 Excel」后再执行
